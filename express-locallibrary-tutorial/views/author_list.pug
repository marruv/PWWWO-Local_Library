extends layout

block content
  h1= title
  
  ul
    each author in author_list
      li 
        a(href=author.url) #{author.name} <!-- Shows author's name -->
        |  (#{author.lifespan}) <!-- Shows author's date of birth and date of death if author has deceased -->

    else
      li There are no authors.